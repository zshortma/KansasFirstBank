/**
 *
 * CompareIndicator displays the visible control and hosts the hidden range input.
 *
 * [Live Demo](https://www.primevue.dev/compare/)
 *
 * @module compareindicator
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import type { Component, HTMLAttributes, VNode } from 'vue';

export declare type CompareIndicatorPassThroughOptionType = CompareIndicatorPassThroughAttributes | ((options: CompareIndicatorPassThroughMethodOptions) => CompareIndicatorPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface CompareIndicatorPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: CompareIndicatorProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link CompareIndicatorProps.pt}
 */
export interface CompareIndicatorPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: CompareIndicatorPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface CompareIndicatorPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in CompareIndicator component.
 */
export interface CompareIndicatorProps {
    /**
     * Use to change the HTML tag of root element.
     * @defaultValue SPAN
     */
    as?: string | Component | undefined;
    /**
     * When enabled, it changes the default rendered element for the one passed as a child element.
     * @defaultValue false
     */
    asChild?: boolean | undefined;
    /**
     * Id of the hidden input.
     */
    inputId?: string | undefined;
    /**
     * Inline style of the hidden input.
     */
    inputStyle?: any;
    /**
     * Style class of the hidden input.
     */
    inputClass?: any;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {CompareIndicatorPassThroughOptions}
     */
    pt?: PassThrough<CompareIndicatorPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Root element attributes forwarded to the consumer when `asChild` is enabled.
 */
export interface CompareIndicatorA11yAttrs extends HTMLAttributes {}

/**
 * Defines valid slots in CompareIndicator component.
 */
export interface CompareIndicatorSlots {
    /**
     * Default slot, place the indicator's visual content here.
     * Receives scope props when `asChild` is enabled.
     * @param {Object} scope - default slot's params.
     */
    default(scope: {
        /**
         * Computed class name for the root.
         */
        class: string | object | Array<string | object>;
        /**
         * Attributes to spread on the consumer's root element.
         */
        a11yAttrs: CompareIndicatorA11yAttrs;
    }): VNode[];
}

/**
 * Defines valid emits in CompareIndicator component.
 */
export interface CompareIndicatorEmitsOptions {}

export declare type CompareIndicatorEmits = EmitFn<CompareIndicatorEmitsOptions>;

/**
 * **PrimeVue - CompareIndicator**
 *
 * _CompareIndicator displays the visible control and hosts the hidden range input._
 *
 * [Live Demo](https://www.primevue.dev/compare/)
 * --- ---
 * ![PrimeVue](https://primefaces.org/cdn/primevue/images/logo-100.png)
 *
 * @group Component
 *
 */
declare const CompareIndicator: DefineComponent<CompareIndicatorProps, CompareIndicatorSlots, CompareIndicatorEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        CompareIndicator: DefineComponent<CompareIndicatorProps, CompareIndicatorSlots, CompareIndicatorEmits>;
    }
}

export default CompareIndicator;
