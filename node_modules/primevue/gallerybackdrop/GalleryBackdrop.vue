<template>
    <component v-if="!asChild" :is="as" :class="cx('root')" v-bind="ptmi('root')">
        <slot />
    </component>
    <slot v-else :class="cx('root')" />
</template>

<script>
import BaseGalleryBackdrop from './BaseGalleryBackdrop.vue';

export default {
    name: 'GalleryBackdrop',
    extends: BaseGalleryBackdrop,
    inheritAttrs: false,
    inject: ['$pcGallery']
};
</script>
