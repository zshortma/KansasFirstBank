/**
 *
 * GalleryFlipX flips the active item horizontally.
 *
 * [Live Demo](https://www.primevue.dev/gallery/)
 *
 * @module galleryflipx
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import type { ButtonHTMLAttributes, Component, VNode } from 'vue';

export declare type GalleryFlipXPassThroughOptionType = GalleryFlipXPassThroughAttributes | ((options: GalleryFlipXPassThroughMethodOptions) => GalleryFlipXPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface GalleryFlipXPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: GalleryFlipXProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link GalleryFlipXProps.pt}
 */
export interface GalleryFlipXPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: GalleryFlipXPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface GalleryFlipXPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in GalleryFlipX component.
 */
export interface GalleryFlipXProps {
    /**
     * Use to change the HTML tag of root element.
     * @defaultValue BUTTON
     */
    as?: string | Component | undefined;
    /**
     * When enabled, it changes the default rendered element for the one passed as a child element.
     * @defaultValue false
     */
    asChild?: boolean | undefined;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     */
    pt?: PassThrough<GalleryFlipXPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Root element attributes forwarded to the consumer when `asChild` is enabled.
 */
export interface GalleryFlipXA11yAttrs extends ButtonHTMLAttributes {
    /**
     * Marker attribute used by Gallery to identify the dispatched action.
     */
    'data-action': 'flip-x';
    /**
     * Click handler that dispatches the `flip-x` action to the active item.
     */
    onClick: (event: MouseEvent) => void;
}

/**
 * Defines valid slots in GalleryFlipX component.
 */
export interface GalleryFlipXSlots {
    /**
     * Custom content template. Receives scope props when `asChild` is enabled.
     * @param {Object} scope - default slot's params.
     */
    default(scope: {
        /**
         * Computed class name for the button root.
         */
        class: string | object | Array<string | object>;
        /**
         * Click handler to spread on the consumer's root element.
         */
        onClick: (event: MouseEvent) => void;
    }): VNode[];
}

/**
 * Defines valid emits in GalleryFlipX component.
 */
export interface GalleryFlipXEmitsOptions {}

export declare type GalleryFlipXEmits = EmitFn<GalleryFlipXEmitsOptions>;

/**
 * **PrimeVue - GalleryFlipX**
 *
 * _GalleryFlipX is a helper component for Gallery component._
 *
 * [Live Demo](https://www.primevue.dev/gallery/)
 * --- ---
 * ![PrimeVue](https://primefaces.org/cdn/primevue/images/logo-100.png)
 *
 * @group Component
 *
 */
declare const GalleryFlipX: DefineComponent<GalleryFlipXProps, GalleryFlipXSlots, GalleryFlipXEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        GalleryFlipX: DefineComponent<GalleryFlipXProps, GalleryFlipXSlots, GalleryFlipXEmits>;
    }
}

export default GalleryFlipX;
