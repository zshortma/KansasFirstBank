<template>
    <component v-if="!asChild" :is="as" :class="cx('root')" v-bind="ptmi('root')">
        <slot />
    </component>
    <slot v-else :class="cx('root')" />
</template>

<script>
import BaseGalleryHeader from './BaseGalleryHeader.vue';

export default {
    name: 'GalleryHeader',
    extends: BaseGalleryHeader,
    inheritAttrs: false,
    inject: ['$pcGallery']
};
</script>
