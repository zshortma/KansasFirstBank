/**
 *
 * GalleryPrev is a button that navigates the Gallery to the previous item.
 *
 * [Live Demo](https://www.primevue.dev/gallery/)
 *
 * @module galleryprev
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import type { ButtonHTMLAttributes, Component, VNode } from 'vue';

export declare type GalleryPrevPassThroughOptionType = GalleryPrevPassThroughAttributes | ((options: GalleryPrevPassThroughMethodOptions) => GalleryPrevPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface GalleryPrevPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: GalleryPrevProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link GalleryPrevProps.pt}
 */
export interface GalleryPrevPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: GalleryPrevPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface GalleryPrevPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in GalleryPrev component.
 */
export interface GalleryPrevProps {
    /**
     * Use to change the HTML tag of root element.
     * @defaultValue BUTTON
     */
    as?: string | Component | undefined;
    /**
     * When enabled, it changes the default rendered element for the one passed as a child element.
     * @defaultValue false
     */
    asChild?: boolean | undefined;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     */
    pt?: PassThrough<GalleryPrevPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Root element attributes forwarded to the consumer when `asChild` is enabled.
 */
export interface GalleryPrevA11yAttrs extends ButtonHTMLAttributes {
    /**
     * Click handler that navigates Gallery to the previous item.
     */
    onClick: (event: MouseEvent) => void;
}

/**
 * Defines valid slots in GalleryPrev component.
 */
export interface GalleryPrevSlots {
    /**
     * Custom content template. Receives scope props when `asChild` is enabled.
     * @param {Object} scope - default slot's params.
     */
    default(scope: {
        /**
         * Computed class name for the button root.
         */
        class: string | object | Array<string | object>;
        /**
         * Click handler to spread on the consumer's root element.
         */
        onClick: (event: MouseEvent) => void;
    }): VNode[];
}

/**
 * Defines valid emits in GalleryPrev component.
 */
export interface GalleryPrevEmitsOptions {}

export declare type GalleryPrevEmits = EmitFn<GalleryPrevEmitsOptions>;

/**
 * **PrimeVue - GalleryPrev**
 *
 * _GalleryPrev is a helper component for Gallery component._
 *
 * [Live Demo](https://www.primevue.dev/gallery/)
 * --- ---
 * ![PrimeVue](https://primefaces.org/cdn/primevue/images/logo-100.png)
 *
 * @group Component
 *
 */
declare const GalleryPrev: DefineComponent<GalleryPrevProps, GalleryPrevSlots, GalleryPrevEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        GalleryPrev: DefineComponent<GalleryPrevProps, GalleryPrevSlots, GalleryPrevEmits>;
    }
}

export default GalleryPrev;
