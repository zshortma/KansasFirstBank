<template>
    <component v-if="!asChild" :is="as" :class="cx('root')" v-bind="ptmi('root')">
        <slot />
    </component>
    <slot v-else :class="cx('root')" />
</template>

<script>
import BaseGalleryThumbnailContent from './BaseGalleryThumbnailContent.vue';

export default {
    name: 'GalleryThumbnailContent',
    extends: BaseGalleryThumbnailContent,
    inheritAttrs: false,
    inject: ['$pcGallery']
};
</script>
