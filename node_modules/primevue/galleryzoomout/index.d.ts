/**
 *
 * GalleryZoomOut dispatches the `zoom-out` action to the active item. The button is automatically disabled
 * when the active item is at its normal scale.
 *
 * [Live Demo](https://www.primevue.dev/gallery/)
 *
 * @module galleryzoomout
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import type { ButtonHTMLAttributes, Component, VNode } from 'vue';

export declare type GalleryZoomOutPassThroughOptionType = GalleryZoomOutPassThroughAttributes | ((options: GalleryZoomOutPassThroughMethodOptions) => GalleryZoomOutPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface GalleryZoomOutPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: GalleryZoomOutProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link GalleryZoomOutProps.pt}
 */
export interface GalleryZoomOutPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: GalleryZoomOutPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface GalleryZoomOutPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in GalleryZoomOut component.
 */
export interface GalleryZoomOutProps {
    /**
     * Use to change the HTML tag of root element.
     * @defaultValue BUTTON
     */
    as?: string | Component | undefined;
    /**
     * When enabled, it changes the default rendered element for the one passed as a child element.
     * @defaultValue false
     */
    asChild?: boolean | undefined;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     */
    pt?: PassThrough<GalleryZoomOutPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Root element attributes forwarded to the consumer when `asChild` is enabled.
 */
export interface GalleryZoomOutA11yAttrs extends ButtonHTMLAttributes {
    /**
     * Marker attribute used by Gallery to identify the dispatched action.
     */
    'data-action': 'zoom-out';
    /**
     * Disabled when the active item is at its normal scale (no zoom to undo).
     */
    disabled?: boolean;
    /**
     * Click handler that dispatches the `zoom-out` action to the active item.
     */
    onClick: (event: MouseEvent) => void;
}

/**
 * Defines valid slots in GalleryZoomOut component.
 */
export interface GalleryZoomOutSlots {
    /**
     * Custom content template. Receives scope props when `asChild` is enabled.
     * @param {Object} scope - default slot's params.
     */
    default(scope: {
        /**
         * Computed class name for the button root.
         */
        class: string | object | Array<string | object>;
        /**
         * Click handler to spread on the consumer's root element.
         */
        onClick: (event: MouseEvent) => void;
        /**
         * Disabled state — true when the active item is at normal scale.
         */
        disabled: boolean;
    }): VNode[];
}

/**
 * Defines valid emits in GalleryZoomOut component.
 */
export interface GalleryZoomOutEmitsOptions {}

export declare type GalleryZoomOutEmits = EmitFn<GalleryZoomOutEmitsOptions>;

/**
 * **PrimeVue - GalleryZoomOut**
 *
 * _GalleryZoomOut is a helper component for Gallery component._
 *
 * [Live Demo](https://www.primevue.dev/gallery/)
 * --- ---
 * ![PrimeVue](https://primefaces.org/cdn/primevue/images/logo-100.png)
 *
 * @group Component
 *
 */
declare const GalleryZoomOut: DefineComponent<GalleryZoomOutProps, GalleryZoomOutSlots, GalleryZoomOutEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        GalleryZoomOut: DefineComponent<GalleryZoomOutProps, GalleryZoomOutSlots, GalleryZoomOutEmits>;
    }
}

export default GalleryZoomOut;
