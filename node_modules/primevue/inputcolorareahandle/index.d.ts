/**
 *
 * 2D handle for `InputColorArea`. Reports current saturation/brightness via ARIA and forwards
 * arrow-key adjustments to the parent `InputColor` via inject.
 *
 * @module inputcolorareahandle
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import type { Component, VNode } from 'vue';

/**
 * Custom passthrough(pt) option type, used to forward attributes via {@link InputColorAreaHandlePassThroughOptions}.
 */
export declare type InputColorAreaHandlePassThroughOptionType = InputColorAreaHandlePassThroughAttributes | ((options: InputColorAreaHandlePassThroughMethodOptions) => InputColorAreaHandlePassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface InputColorAreaHandlePassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: InputColorAreaHandleProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link InputColorAreaHandleProps.pt}
 */
export interface InputColorAreaHandlePassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: InputColorAreaHandlePassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface InputColorAreaHandlePassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in InputColorAreaHandle component.
 */
export interface InputColorAreaHandleProps {
    /**
     * Use to change the HTML tag of root element.
     * @defaultValue DIV
     */
    as?: string | Component | undefined;
    /**
     * When enabled, it changes the default rendered element for the one passed as a child element.
     * @defaultValue false
     */
    asChild?: boolean | undefined;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {InputColorAreaHandlePassThroughOptions}
     */
    pt?: PassThrough<InputColorAreaHandlePassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in InputColorAreaHandle component.
 */
export interface InputColorAreaHandleSlots {
    /**
     * Custom root element template. Receives scope props when `asChild` is enabled.
     * @param {Object} scope - default slot's params.
     */
    default(scope: {
        /**
         * Style class of the area handle root.
         */
        class: string;
        /**
         * Attributes to spread on the consumer's root element.
         */
        a11yAttrs: Record<string, unknown>;
    }): VNode[];
}

/**
 * Defines valid emits in InputColorAreaHandle component.
 */
export interface InputColorAreaHandleEmitsOptions {}

/**
 * Type alias for emits resolved from {@link InputColorAreaHandleEmitsOptions}.
 */
export declare type InputColorAreaHandleEmits = EmitFn<InputColorAreaHandleEmitsOptions>;

/**
 * **PrimeVue - InputColorAreaHandle**
 *
 * _2D handle rendered inside `InputColorArea`._
 *
 * [Live Demo](https://www.primevue.dev/inputcolor/)
 * --- ---
 * ![PrimeVue](https://primefaces.org/cdn/primevue/images/logo-100.png)
 *
 * @group Component
 *
 */
declare const InputColorAreaHandle: DefineComponent<InputColorAreaHandleProps, InputColorAreaHandleSlots, InputColorAreaHandleEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        InputColorAreaHandle: DefineComponent<InputColorAreaHandleProps, InputColorAreaHandleSlots, InputColorAreaHandleEmits>;
    }
}

export default InputColorAreaHandle;
