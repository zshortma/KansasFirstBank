/**
 *
 * Color preview swatch reflecting the parent `InputColor` value. Renders an `InputColorTransparencyGrid`
 * + `InputColorSwatchBackground` by default; override the slot to compose differently.
 *
 * @module inputcolorswatch
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import type { Component, VNode } from 'vue';

/**
 * Custom passthrough(pt) option type, used to forward attributes via {@link InputColorSwatchPassThroughOptions}.
 */
export declare type InputColorSwatchPassThroughOptionType = InputColorSwatchPassThroughAttributes | ((options: InputColorSwatchPassThroughMethodOptions) => InputColorSwatchPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface InputColorSwatchPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: InputColorSwatchProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link InputColorSwatchProps.pt}
 */
export interface InputColorSwatchPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: InputColorSwatchPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface InputColorSwatchPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in InputColorSwatch component.
 */
export interface InputColorSwatchProps {
    /**
     * Use to change the HTML tag of root element.
     * @defaultValue DIV
     */
    as?: string | Component | undefined;
    /**
     * When enabled, it changes the default rendered element for the one passed as a child element.
     * @defaultValue false
     */
    asChild?: boolean | undefined;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {InputColorSwatchPassThroughOptions}
     */
    pt?: PassThrough<InputColorSwatchPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in InputColorSwatch component.
 */
export interface InputColorSwatchSlots {
    /**
     * Default slot, place transparency grid and swatch background (e.g. `InputColorTransparencyGrid` + `InputColorSwatchBackground`) here.
     * @param {Object} scope - default slot's params.
     */
    default(scope: {
        /**
         * Style class of the swatch root.
         */
        class: string;
        /**
         * Attributes to spread on the consumer's root element.
         */
        a11yAttrs: Record<string, unknown>;
    }): VNode[];
}

/**
 * Defines valid emits in InputColorSwatch component.
 */
export interface InputColorSwatchEmitsOptions {}

/**
 * Type alias for emits resolved from {@link InputColorSwatchEmitsOptions}.
 */
export declare type InputColorSwatchEmits = EmitFn<InputColorSwatchEmitsOptions>;

/**
 * **PrimeVue - InputColorSwatch**
 *
 * _Color preview swatch reflecting the parent `InputColor` value._
 *
 * [Live Demo](https://www.primevue.dev/inputcolor/)
 * --- ---
 * ![PrimeVue](https://primefaces.org/cdn/primevue/images/logo-100.png)
 *
 * @group Component
 *
 */
declare const InputColorSwatch: DefineComponent<InputColorSwatchProps, InputColorSwatchSlots, InputColorSwatchEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        InputColorSwatch: DefineComponent<InputColorSwatchProps, InputColorSwatchSlots, InputColorSwatchEmits>;
    }
}

export default InputColorSwatch;
