/**
 *
 * Solid color fill rendered inside `InputColorSwatch`. The fill is supplied via the CSS custom
 * property `--swatch-background` set on the swatch root.
 *
 * @module inputcolorswatchbackground
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import type { Component, VNode } from 'vue';

/**
 * Custom passthrough(pt) option type, used to forward attributes via {@link InputColorSwatchBackgroundPassThroughOptions}.
 */
export declare type InputColorSwatchBackgroundPassThroughOptionType =
    InputColorSwatchBackgroundPassThroughAttributes | ((options: InputColorSwatchBackgroundPassThroughMethodOptions) => InputColorSwatchBackgroundPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface InputColorSwatchBackgroundPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: InputColorSwatchBackgroundProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link InputColorSwatchBackgroundProps.pt}
 */
export interface InputColorSwatchBackgroundPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: InputColorSwatchBackgroundPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface InputColorSwatchBackgroundPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in InputColorSwatchBackground component.
 */
export interface InputColorSwatchBackgroundProps {
    /**
     * Use to change the HTML tag of root element.
     * @defaultValue DIV
     */
    as?: string | Component | undefined;
    /**
     * When enabled, it changes the default rendered element for the one passed as a child element.
     * @defaultValue false
     */
    asChild?: boolean | undefined;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {InputColorSwatchBackgroundPassThroughOptions}
     */
    pt?: PassThrough<InputColorSwatchBackgroundPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in InputColorSwatchBackground component.
 */
export interface InputColorSwatchBackgroundSlots {
    /**
     * Custom root element template. Receives scope props when `asChild` is enabled.
     * @param {Object} scope - default slot's params.
     */
    default(scope: {
        /**
         * Style class of the swatch background root.
         */
        class: string;
        /**
         * Attributes to spread on the consumer's root element.
         */
        a11yAttrs: Record<string, unknown>;
    }): VNode[];
}

/**
 * Defines valid emits in InputColorSwatchBackground component.
 */
export interface InputColorSwatchBackgroundEmitsOptions {}

/**
 * Type alias for emits resolved from {@link InputColorSwatchBackgroundEmitsOptions}.
 */
export declare type InputColorSwatchBackgroundEmits = EmitFn<InputColorSwatchBackgroundEmitsOptions>;

/**
 * **PrimeVue - InputColorSwatchBackground**
 *
 * _Solid color fill rendered inside `InputColorSwatch`._
 *
 * [Live Demo](https://www.primevue.dev/inputcolor/)
 * --- ---
 * ![PrimeVue](https://primefaces.org/cdn/primevue/images/logo-100.png)
 *
 * @group Component
 *
 */
declare const InputColorSwatchBackground: DefineComponent<InputColorSwatchBackgroundProps, InputColorSwatchBackgroundSlots, InputColorSwatchBackgroundEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        InputColorSwatchBackground: DefineComponent<InputColorSwatchBackgroundProps, InputColorSwatchBackgroundSlots, InputColorSwatchBackgroundEmits>;
    }
}

export default InputColorSwatchBackground;
