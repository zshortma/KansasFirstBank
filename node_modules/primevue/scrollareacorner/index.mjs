import { mergeProps, openBlock, createElementBlock, Fragment, createBlock, resolveDynamicComponent, withCtx, renderSlot, normalizeClass, createCommentVNode } from 'vue';
import BaseComponent from '@primevue/core/basecomponent';
import ScrollAreaCornerStyle from 'primevue/scrollareacorner/style';

var script$1 = {
  name: 'BaseScrollAreaCorner',
  "extends": BaseComponent,
  props: {
    as: {
      type: [String, Object],
      "default": 'DIV'
    },
    asChild: {
      type: Boolean,
      "default": false
    }
  },
  style: ScrollAreaCornerStyle,
  provide: function provide() {
    return {
      $pcScrollAreaCorner: this,
      $parentInstance: this
    };
  }
};

var script = {
  name: 'ScrollAreaCorner',
  "extends": script$1,
  inheritAttrs: false,
  inject: ['$pcScrollArea'],
  computed: {
    shouldRender: function shouldRender() {
      var sa = this.$pcScrollArea;
      if (!sa) return false;
      var meta = sa.scrollMeta;
      return !meta.hiddenX && !meta.hiddenY;
    },
    cornerStyle: function cornerStyle() {
      return {
        width: 'var(--px-corner-width)',
        height: 'var(--px-corner-height)'
      };
    },
    attrs: function attrs() {
      return mergeProps(this.a11yAttrs, this.ptmi('root'));
    },
    a11yAttrs: function a11yAttrs() {
      return {
        style: this.cornerStyle
      };
    }
  }
};

function render(_ctx, _cache, $props, $setup, $data, $options) {
  return $options.shouldRender ? (openBlock(), createElementBlock(Fragment, {
    key: 0
  }, [!_ctx.asChild ? (openBlock(), createBlock(resolveDynamicComponent(_ctx.as), mergeProps({
    key: 0,
    "class": _ctx.cx('root'),
    style: _ctx.sx('root')
  }, $options.attrs), {
    "default": withCtx(function () {
      return [renderSlot(_ctx.$slots, "default")];
    }),
    _: 3
  }, 16, ["class", "style"])) : renderSlot(_ctx.$slots, "default", {
    "class": normalizeClass(_ctx.cx('root')),
    a11yAttrs: $options.a11yAttrs
  }, undefined, undefined, 1)], 64)) : createCommentVNode("", true);
}

script.render = render;

export { script as default };
