/**
 *
 * SidebarContent component - part of the headless Sidebar compound component family.
 *
 * @module sidebarcontent
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import { VNode } from 'vue';

export declare type SidebarContentPassThroughOptionType = SidebarContentPassThroughAttributes | ((options: SidebarContentPassThroughMethodOptions) => SidebarContentPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface SidebarContentPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: SidebarContentProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link SidebarContentProps.pt}
 */
export interface SidebarContentPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: SidebarContentPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface SidebarContentPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in SidebarContent component.
 */
export interface SidebarContentProps {
    /**
     * The element or component to render. Defaults to a sensible HTML element.
     */
    as?: string | object;
    /**
     * When true, renders the slot content as the root element with merged props.
     * @defaultValue false
     */
    asChild?: boolean;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {SidebarContentPassThroughOptions}
     */
    pt?: PassThrough<SidebarContentPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in SidebarContent component.
 */
export interface SidebarContentSlots {
    /**
     * Default slot.
     *
     * The `scope` argument is only provided when `asChild` is `true`. In that mode
     * the consumer must spread `a11yAttrs` and apply `class` on the rendered root.
     * @param {Object} scope - default slot's params.
     */
    default(scope?: {
        /**
         * Class name of the root element.
         */
        class: string;
        /**
         * Accessibility data attributes and event handlers to spread on the rendered root in asChild mode.
         */
        a11yAttrs: Record<string, any>;
    }): VNode[];
}

/**
 * Defines valid emits in SidebarContent component.
 */
export interface SidebarContentEmitsOptions {}

export declare type SidebarContentEmits = EmitFn<SidebarContentEmitsOptions>;

/**
 * **PrimeVue - SidebarContent**
 *
 * _SidebarContent component - part of the headless Sidebar compound component family._
 *
 * @group Component
 *
 */
declare const SidebarContent: DefineComponent<SidebarContentProps, SidebarContentSlots, SidebarContentEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        SidebarContent: DefineComponent<SidebarContentProps, SidebarContentSlots, SidebarContentEmits>;
    }
}

export default SidebarContent;
