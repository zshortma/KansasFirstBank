<template>
    <component v-if="!asChild" :is="as" :class="cx('root')" :type="as === 'BUTTON' || as === 'button' ? 'button' : undefined" v-bind="ptmi('root')">
        <slot />
    </component>
    <slot v-else :class="cx('root')" />
</template>

<script>
import BaseSidebarGroupAction from './BaseSidebarGroupAction.vue';

export default {
    name: 'SidebarGroupAction',
    extends: BaseSidebarGroupAction,
    inheritAttrs: false
};
</script>
