/**
 *
 * SidebarGroupLabel component - part of the headless Sidebar compound component family.
 *
 * @module sidebargrouplabel
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import { VNode } from 'vue';

export declare type SidebarGroupLabelPassThroughOptionType = SidebarGroupLabelPassThroughAttributes | ((options: SidebarGroupLabelPassThroughMethodOptions) => SidebarGroupLabelPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface SidebarGroupLabelPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: SidebarGroupLabelProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link SidebarGroupLabelProps.pt}
 */
export interface SidebarGroupLabelPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: SidebarGroupLabelPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface SidebarGroupLabelPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in SidebarGroupLabel component.
 */
export interface SidebarGroupLabelProps {
    /**
     * The element or component to render. Defaults to a sensible HTML element.
     */
    as?: string | object;
    /**
     * When true, renders the slot content as the root element with merged props.
     * @defaultValue false
     */
    asChild?: boolean;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {SidebarGroupLabelPassThroughOptions}
     */
    pt?: PassThrough<SidebarGroupLabelPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in SidebarGroupLabel component.
 */
export interface SidebarGroupLabelSlots {
    /**
     * Default slot.
     *
     * The `scope` argument is only provided when `asChild` is `true`. In that mode
     * the consumer must spread `a11yAttrs` and apply `class` on the rendered root.
     * @param {Object} scope - default slot's params.
     */
    default(scope?: {
        /**
         * Class name of the root element.
         */
        class: string;
        /**
         * Accessibility data attributes and event handlers to spread on the rendered root in asChild mode.
         */
        a11yAttrs: Record<string, any>;
    }): VNode[];
}

/**
 * Defines valid emits in SidebarGroupLabel component.
 */
export interface SidebarGroupLabelEmitsOptions {}

export declare type SidebarGroupLabelEmits = EmitFn<SidebarGroupLabelEmitsOptions>;

/**
 * **PrimeVue - SidebarGroupLabel**
 *
 * _SidebarGroupLabel component - part of the headless Sidebar compound component family._
 *
 * @group Component
 *
 */
declare const SidebarGroupLabel: DefineComponent<SidebarGroupLabelProps, SidebarGroupLabelSlots, SidebarGroupLabelEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        SidebarGroupLabel: DefineComponent<SidebarGroupLabelProps, SidebarGroupLabelSlots, SidebarGroupLabelEmits>;
    }
}

export default SidebarGroupLabel;
