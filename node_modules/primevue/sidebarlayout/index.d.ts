/**
 *
 * SidebarLayout component - part of the headless Sidebar compound component family.
 *
 * @module sidebarlayout
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import { VNode } from 'vue';

export declare type SidebarLayoutPassThroughOptionType = SidebarLayoutPassThroughAttributes | ((options: SidebarLayoutPassThroughMethodOptions) => SidebarLayoutPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface SidebarLayoutPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: SidebarLayoutProps;
    /**
     * Defines current inline state.
     */
    state: SidebarLayoutState;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link SidebarLayoutProps.pt}
 */
export interface SidebarLayoutPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: SidebarLayoutPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface SidebarLayoutPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines current inline state in SidebarLayout component.
 */
export interface SidebarLayoutState {
    /**
     * Registered sidebar entries keyed by sidebar id.
     */
    registryMap: Map<string, any>;
}

/**
 * Defines valid properties in SidebarLayout component.
 */
export interface SidebarLayoutProps {
    /**
     * The element or component to render. Defaults to a sensible HTML element.
     */
    as?: string | object;
    /**
     * When true, renders the slot content as the root element with merged props.
     * @defaultValue false
     */
    asChild?: boolean;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {SidebarLayoutPassThroughOptions}
     */
    pt?: PassThrough<SidebarLayoutPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in SidebarLayout component.
 */
export interface SidebarLayoutSlots {
    /**
     * Default slot.
     *
     * The `scope` argument is only provided when `asChild` is `true`. In that mode
     * the consumer must apply `class` on the rendered root element.
     * @param {Object} scope - default slot's params.
     */
    default(scope?: {
        /**
         * Class name of the root element.
         */
        class: string;
    }): VNode[];
}

/**
 * Defines valid emits in SidebarLayout component.
 */
export interface SidebarLayoutEmitsOptions {}

export declare type SidebarLayoutEmits = EmitFn<SidebarLayoutEmitsOptions>;

/**
 * **PrimeVue - SidebarLayout**
 *
 * _SidebarLayout component - part of the headless Sidebar compound component family._
 *
 * @group Component
 *
 */
declare const SidebarLayout: DefineComponent<SidebarLayoutProps, SidebarLayoutSlots, SidebarLayoutEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        SidebarLayout: DefineComponent<SidebarLayoutProps, SidebarLayoutSlots, SidebarLayoutEmits>;
    }
}

export default SidebarLayout;
