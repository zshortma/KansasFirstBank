/**
 *
 * SidebarMain component - part of the headless Sidebar compound component family.
 *
 * @module sidebarmain
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import { VNode } from 'vue';

export declare type SidebarMainPassThroughOptionType = SidebarMainPassThroughAttributes | ((options: SidebarMainPassThroughMethodOptions) => SidebarMainPassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface SidebarMainPassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: SidebarMainProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link SidebarMainProps.pt}
 */
export interface SidebarMainPassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: SidebarMainPassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface SidebarMainPassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in SidebarMain component.
 */
export interface SidebarMainProps {
    /**
     * The element or component to render. Defaults to a sensible HTML element.
     */
    as?: string | object;
    /**
     * When true, renders the slot content as the root element with merged props.
     * @defaultValue false
     */
    asChild?: boolean;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {SidebarMainPassThroughOptions}
     */
    pt?: PassThrough<SidebarMainPassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in SidebarMain component.
 */
export interface SidebarMainSlots {
    /**
     * Default slot.
     *
     * The `scope` argument is only provided when `asChild` is `true`. In that mode
     * the consumer must apply `class` on the rendered root element.
     * @param {Object} scope - default slot's params.
     */
    default(scope?: {
        /**
         * Class name of the root element.
         */
        class: string;
    }): VNode[];
}

/**
 * Defines valid emits in SidebarMain component.
 */
export interface SidebarMainEmitsOptions {}

export declare type SidebarMainEmits = EmitFn<SidebarMainEmitsOptions>;

/**
 * **PrimeVue - SidebarMain**
 *
 * _SidebarMain component - part of the headless Sidebar compound component family._
 *
 * @group Component
 *
 */
declare const SidebarMain: DefineComponent<SidebarMainProps, SidebarMainSlots, SidebarMainEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        SidebarMain: DefineComponent<SidebarMainProps, SidebarMainSlots, SidebarMainEmits>;
    }
}

export default SidebarMain;
