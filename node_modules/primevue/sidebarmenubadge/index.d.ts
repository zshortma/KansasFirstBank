/**
 *
 * SidebarMenuBadge component - part of the headless Sidebar compound component family.
 *
 * @module sidebarmenubadge
 *
 */
import type { DefineComponent, DesignToken, EmitFn, PassThrough } from '@primevue/core';
import type { ComponentHooks } from '@primevue/core/basecomponent';
import type { PassThroughOptions } from 'primevue/passthrough';
import { VNode } from 'vue';

export declare type SidebarMenuBadgePassThroughOptionType = SidebarMenuBadgePassThroughAttributes | ((options: SidebarMenuBadgePassThroughMethodOptions) => SidebarMenuBadgePassThroughAttributes | string) | string | null | undefined;

/**
 * Custom passthrough(pt) option method.
 */
export interface SidebarMenuBadgePassThroughMethodOptions {
    /**
     * Defines instance.
     */
    instance: any;
    /**
     * Defines valid properties.
     */
    props: SidebarMenuBadgeProps;
    /**
     * Defines valid attributes.
     */
    attrs: any;
    /**
     * Defines parent options.
     */
    parent: any;
    /**
     * Defines passthrough(pt) options in global config.
     */
    global: object | undefined;
}

/**
 * Custom passthrough(pt) options.
 * @see {@link SidebarMenuBadgeProps.pt}
 */
export interface SidebarMenuBadgePassThroughOptions {
    /**
     * Used to pass attributes to the root's DOM element.
     */
    root?: SidebarMenuBadgePassThroughOptionType;
    /**
     * Used to manage all lifecycle hooks.
     * @see {@link BaseComponent.ComponentHooks}
     */
    hooks?: ComponentHooks;
}

/**
 * Custom passthrough attributes for each DOM elements.
 */
export interface SidebarMenuBadgePassThroughAttributes {
    [key: string]: any;
}

/**
 * Defines valid properties in SidebarMenuBadge component.
 */
export interface SidebarMenuBadgeProps {
    /**
     * The element or component to render. Defaults to a sensible HTML element.
     */
    as?: string | object;
    /**
     * When true, renders the slot content as the root element with merged props.
     * @defaultValue false
     */
    asChild?: boolean;
    /**
     * It generates scoped CSS variables using design tokens for the component.
     */
    dt?: DesignToken<any>;
    /**
     * Used to pass attributes to DOM elements inside the component.
     * @type {SidebarMenuBadgePassThroughOptions}
     */
    pt?: PassThrough<SidebarMenuBadgePassThroughOptions>;
    /**
     * Used to configure passthrough(pt) options of the component.
     * @type {PassThroughOptions}
     */
    ptOptions?: PassThroughOptions;
    /**
     * When enabled, it removes component related styles in the core.
     * @defaultValue false
     */
    unstyled?: boolean;
}

/**
 * Defines valid slots in SidebarMenuBadge component.
 */
export interface SidebarMenuBadgeSlots {
    /**
     * Default slot.
     *
     * The `scope` argument is only provided when `asChild` is `true`. In that mode
     * the consumer must spread `a11yAttrs` and apply `class` on the rendered root.
     * @param {Object} scope - default slot's params.
     */
    default(scope?: {
        /**
         * Class name of the root element.
         */
        class: string;
        /**
         * Accessibility data attributes and event handlers to spread on the rendered root in asChild mode.
         */
        a11yAttrs: Record<string, any>;
    }): VNode[];
}

/**
 * Defines valid emits in SidebarMenuBadge component.
 */
export interface SidebarMenuBadgeEmitsOptions {}

export declare type SidebarMenuBadgeEmits = EmitFn<SidebarMenuBadgeEmitsOptions>;

/**
 * **PrimeVue - SidebarMenuBadge**
 *
 * _SidebarMenuBadge component - part of the headless Sidebar compound component family._
 *
 * @group Component
 *
 */
declare const SidebarMenuBadge: DefineComponent<SidebarMenuBadgeProps, SidebarMenuBadgeSlots, SidebarMenuBadgeEmits>;

declare module 'vue' {
    export interface GlobalComponents {
        SidebarMenuBadge: DefineComponent<SidebarMenuBadgeProps, SidebarMenuBadgeSlots, SidebarMenuBadgeEmits>;
    }
}

export default SidebarMenuBadge;
